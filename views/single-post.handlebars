{{> post-info post}}

{{#if loggedIn}}
  <form class='comment-form container'>
    <div>
      <textarea class="card w-100 h-10" name='comment-body'></textarea>
    </div>

    <div class="container">
      <button type='submit' class="btn w-25">add comment</button>
      <button type='button' class='upvote-btn btn w-25'>upvote</button>
    </div>
  </form>
{{/if}}

{{>comments post.comments}}

{{#if loggedIn}}
  <script src='/javascript/comment.js'></script>
  <script src='/javascript/upvote.js'></script>
{{/if}}